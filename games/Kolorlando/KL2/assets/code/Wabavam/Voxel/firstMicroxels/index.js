const ROOT = "./";

export const firstMicroxelVoxelFiles = [
    "blue_slab.voxel",
    "blue_stair.voxel",
    "table.voxel",
    "sun_box.voxel",
    "Flower.voxel"
].map(file => `${ROOT}${file}`);

export default firstMicroxelVoxelFiles;
